@prefix ex: <https://www.bgee.org/sparql/.well-known/sparql-examples/> .
@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .
@prefix schema: <https://schema.org/> .
@prefix sh: <http://www.w3.org/ns/shacl#> .

ex:003 a sh:SPARQLExecutable,
        sh:SPARQLSelectExecutable ;
    rdfs:comment "Coverage of the ORDO terminology wikidata property across 6 languages"@en ;
    sh:prefixes _:sparql_examples_prefixes ;
    sh:select """
PREFIX wikibase: <http://wikiba.se/ontology#>
PREFIX schema: <http://schema.org/>
PREFIX wdt: <http://www.wikidata.org/prop/direct/>
SELECT ?lang ?nItems ?nordo_withWiki
       (?nItems * 1.0 / ?nordo_withWiki AS ?coverageAmongWiki)
WHERE {
  # items that have at least one Wikipedia sitelink
  {
    SELECT (COUNT(DISTINCT ?item) AS ?nsordo_withWiki)
    WHERE {
      ?item wdt:P1550 ?ordoId .
      ?art schema:about ?item ;
            schema:isPartOf ?site .
      ?site wikibase:wikiGroup "wikipedia" .
    }
  }
  # items with an article in each specific language
  {
    SELECT ?lang (COUNT(DISTINCT ?wikidata) AS ?nItems)
    WHERE {
      ?wikidata wdt:P1550 ?ordoId .
      ?article schema:about ?wikidata ;
               schema:inLanguage ?lang ;
               schema:isPartOf ?site .
               ?site wikibase:wikiGroup "wikipedia" .
               VALUES ?lang { "en" "fr" "pt" "de" "es" "nl" }
      
    }
    GROUP BY ?lang
  }
}
ORDER BY DESC(?coverageAmongWiki)

""" ;
    schema:target <https://qlever.dev/wikidata>, <https://query.wikidata.org/>  .
